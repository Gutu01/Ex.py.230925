from calculos import media

numero1 = float(input(f'Digite um número: '))
numero2 = float(input(f'Digite outro número: '))

print(f'A média dos valores é de {media(numero1, numero2)}')

#   O arquivo da biblioteca não deve ser o mesmo da função pois, se isso acontecesse, a biblioteca não seria clara para que ela serve. Ao criar uma bibliteca você deve dar o nome dela a categoria das funções que ela possui, e não dar o nome de uma função expecífica só.